# configs (DRAFT)

## File map
- `unitn_seed_smoke.toml` (`DRAFT`): minimal chapter-2.1 smoke config for seed-only crawl runs.

## Why these files are together
This folder stores reproducible ingestion configuration samples used by runnable commands and docs.
